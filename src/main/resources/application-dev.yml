spring:
  datasource:
    url: jdbc:mysql://localhost:3306/timishelter_dev
    username: timishelter_dev_user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect #org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true